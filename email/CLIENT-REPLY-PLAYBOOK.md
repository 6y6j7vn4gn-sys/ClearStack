# ClearStack — Client email & Zelle courtesy (public-facing)

**Brand:** ClearStack only · **Tone:** calm, professional, confident · **Never** personal names on client email

## Inbox (until domain mail)
| Role | Address | Notes |
|------|---------|-------|
| Public / quotes | `hello.clearstack.ie@gmail.com` *(create once — see SETUP)* | Display name: **ClearStack** |
| After domain | `hello@goclearstack.com` | Forward to same inbox |
| Operator private | Mina’s personal Gmail | Never on website / GBP / client CC as primary |

## Payment framing (legitimacy-safe)
- **Cards / Payment Link** = standard professional checkout (full package price).
- **Zelle** = optional bank-to-bank deposit path with a **small intake courtesy** for faster cleared deposits.
- Same packages · same OCR digital book · same QA · same turnaround standards.
- Do **not** say “cash discount,” “cheaper offline,” or anything that sounds like a workaround.
- Preferred line: *“For clients who prefer bank-to-bank deposit via Zelle, we apply a $50 intake courtesy on the package deposit. Packages and deliverables are identical.”*

### Zelle courtesy (locked offer)
- **$50 off the deposit** when the **50% deposit** is sent via Zelle (not on balance unless you decide later).
- Example: $1,250 package → deposit $625 → Zelle deposit due **$575**.
- Client still receives full ClearStack package (OCR searchable book, index, secure link; USB still +$75 if chosen).
- Always send written quote + invoice ID (`CS-YYYYMMDD-##`) before accepting funds.
- Confirm Zelle name/handle only **after** quote acceptance (never publish personal phone/home on site).

## Signature block (all client mail)
```
ClearStack
Messy stack in. Searchable digital book out.
Inland Empire · Document → searchable PDF packages
hello@… (active ClearStack inbox)
```

## Accounting lock (Accounting bot · 2026-09-06)
- Invoice shows **full package list price** (unchanged).
- Deposit: standard 50% line; if Zelle chosen, add **“Zelle intake courtesy” −$50**.
- Amount due now (Zelle) = (50% of package) − $50.
- Balance later = remaining 50% (courtesy does not shrink the back half).
- Cards / Payment Links: **no −$50**.
- Zelle memo: `CS-YYYYMMDD-## {customer_last}` — match bank only when memo + amount match.
- Never invent Zelle receive name/handle — Mina confirms.
- Client copy: “intake courtesy” / “Zelle deposit courtesy” — not a different package price.
- If client paid card then asks for $50: discretionary — needs Mina send approval.
